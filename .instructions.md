My approach is to write 2 contracts Escrow and Looper.
Escrow:
Pre setup:
- The user will give the Escrow contract an allowance for its crc a.
Escrow will have a method to atomically perform the following:
Args:
- FromUser, fromAmount, fromToken, edgeToken (counterpart of stablecoin in the liquidity pool), receiveToken, liquidityPool address (uniswap v2 router, must swap between edgeToken and receiveToken), path (information necessary to call transferThrough from Escrow contract instance and the Looper contract instance)
Execution:
- Transfer user a CRC to itself.
- Use transferThrough to send "a" to Escrow (it will endup receiving "c" which is the edgeToken) 
- Use the Looper recover function to get the d edgeToken it received with transferThrough
- Use the edgeToken it received to make a swap on the liquidityPool, exchanging edgeToken for receiveToken
- Transfer receiveToken back to the fromUser

Looper:
Pretty simple contract, I'm thinking of something just to serve as a recepient of the transferThrough that allows a function to be called retrieving this funds back to Escrow. 

What do you think about it?